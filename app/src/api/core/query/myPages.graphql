query MyPages($pk: ID) {
  myPages(id: $pk) {
    edges {
      node {
        notebook {
          pk
        }
        pk
        updatedAt
        createdAt
        paperColor
        patternColor
        patternType
        patternSize
        patternSpacing
        patternOpacity
      }
    }
  }
}
