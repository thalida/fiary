query MyPages($pk: ID) {
  myPages(id: $pk) {
    edges {
      node {
        notebook {
          pk
        }
        pk
        updatedAt
        createdAt
        bgColor
        patternStyle
        patternColor
        patternSize
        patternSpacing
      }
    }
  }
}
