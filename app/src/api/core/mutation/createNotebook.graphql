mutation CreateNotebook($bookshelfPk: UUID!, $title: String) {
  createNotebook(input: {bookshelfPk: $bookshelfPk, title: $title}) {
    notebook {
      bookshelf {
        pk
        notebookOrder
      }
      pk
      updatedAt
      createdAt
      title
      pageOrder
    }
  }
}
