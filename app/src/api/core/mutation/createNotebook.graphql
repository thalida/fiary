mutation CreateNotebook($bookshelfId: UUID!, $title: String) {
  createNotebook(input: {bookshelfId: $bookshelfId, title: $title}) {
    notebook {
      bookshelf {
        pk
        notebookOrder
      }
      pk
      id
      updatedAt
      createdAt
      title
      pageOrder
    }
  }
}
